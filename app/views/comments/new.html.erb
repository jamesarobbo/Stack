<!-- Comment Form -->
<h2>Add a comment</h2>

<%= simple_form_for [@story, @comment] do |form| %> <!-- why are we listing @story and @comment? -->

	<%= form.input :body, label: "Comment" %>
	<%= form.button :submit %>

<%end%>	
